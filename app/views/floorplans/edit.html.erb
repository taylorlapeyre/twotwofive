<h1>Step Three</h1>
<h3>Add Some Floorplans</h3>

<%= render 'floorplans/form', :listing_id => @listing.id %>

  <%= f.file_field :image_file %>
  <br>
  <%= f.submit "Upload" %>

<%= link_to "Finish", @listing %>